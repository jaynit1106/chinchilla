import 'package:flutter/material.dart';

class DataEntryScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Data Entry Screen"),
      ),
    );
  }
}
